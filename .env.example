# Copy this file to .env for local dev.
# IMPORTANT: Do NOT commit your real .env to Git.

# -------------------------
# Database (Postgres)
# -------------------------
# Local Docker example:
# DATABASE_URL="postgresql://postgres:pass@localhost:5432/qrdb?schema=public"
# Neon example (pooled / recommended for runtime):
# DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"
DATABASE_URL=""

# Non-pooled connection (recommended for Prisma migrations/DDL with Neon/PgBouncer)
# Neon provides a "non-pooling" / "direct" URL in the Quickstart snippet.
DATABASE_URL_UNPOOLED=""

# -------------------------
# Admin login for /admin
# -------------------------
ADMIN_USER=""
ADMIN_PASS=""

# -------------------------
# Auth (cookie/token signing)
# -------------------------
# 32+ random chars
AUTH_SECRET=""

# -------------------------
# Public config (safe to expose)
# -------------------------
# In production you can set this to your Vercel domain (optional).
# If empty, the app will infer from request headers.
NEXT_PUBLIC_BASE_URL=""

NEXT_PUBLIC_COMPANY_NAME="Hari Chand Anand & Co."
NEXT_PUBLIC_LOGO_URL=""

# Optional defaults
NEXT_PUBLIC_DEFAULT_WA_NUMBER=""
NEXT_PUBLIC_DEFAULT_WA_TEMPLATE=""

# -------------------------
# Spares Installation Reports (Google Sheets CSV)
# -------------------------
# Public CSV URL from Google Sheets (File > Share > Publish to Web > CSV)
# Example (gviz): https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/gviz/tq?tqx=out:csv&sheet=Spares
NEXT_PUBLIC_SPARES_SHEET_CSV_URL=""

# -------------------------
# Centralized Google Sheet (multi-tab)
# -------------------------
# If you use a single global sheet with many tabs, set the Sheet ID here.
# Your per-machine tab name can be stored in Machine.sheetsLink (recommended).
NEXT_PUBLIC_GLOBAL_SHEET_ID=""

# Optional: tab suffix convention for spares tabs (default: __SPARES)
NEXT_PUBLIC_SPARES_TAB_SUFFIX="__SPARES"
